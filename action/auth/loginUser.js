import apiService from "../../services/apiService";

export const keepLogin = async accessToken => {
    try {
        console.log('keep login');
        let isvalid = true;

        return isvalid;
    } catch (e) {
        console.log(e.message);
    }
}

export const newLogin = async tokenId => {
    try {
        return 'idfdsfsoiufoijsf1545'

    } catch (e) {
        console.log(e.message);
    }
}


export const logout = async accessToken => {
    try {
        let isvalid = true;

        return isvalid;
    } catch (e) {
        console.log(e.message);
    }
}

